---
author:    Les Hill
title:      Archive for <%= Time.now.strftime('%Y') %>
created_at: <%= Time.now.to_y %>
archive:    year
filter:     haml
layout:     index
dirty:      true
---
.archives
  %h2= @page.title
  - months = @pages.find(:all, :in_directory => @page.dir, :recursive => true, :sort_by => "created_at", :reverse => true) {|p| p.archive}
  - months.delete(@page)
  %ul
    - months.sort_by{|p| p.created_at.strftime('%m')}.each do |month|
      %li.month
        %a{:href => month.url}= month.created_at.strftime('%B')
