---
filter: haml
---
.archives
  %h2 Archives
  %ul
    - months.group_by{|a| a.created_at.strftime('%Y')}.each do |year, months|
      %li.year
        %a{:href => "/blog/#{year}"}= year
        %ul
          - months.sort_by{|p| p.created_at.strftime('%m')}.each do |month|
            %li.month
              %a{:href => month.url}= month.created_at.strftime('%B')
